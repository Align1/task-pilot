# Task Pilot Environment Configuration
# Copy this file to .env and update with your actual values
# NEVER commit .env to version control!

# ============================================
# CRITICAL: JWT Secret Key
# ============================================
# This is REQUIRED for the server to start
# Must be at least 32 characters long
# Should be cryptographically random
#
# Generate a secure secret with Node.js:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# Or use this online tool (ensure you trust it):
#   https://generate-secret.vercel.app/32
#
JWT_SECRET=REPLACE_THIS_WITH_A_SECURE_RANDOM_STRING_AT_LEAST_32_CHARACTERS_LONG

# ============================================
# Server Configuration
# ============================================
PORT=3001

# ============================================
# Database Configuration (Prisma)
# ============================================
# SQLite (default for development)
DATABASE_URL="file:./dev.db"

# PostgreSQL (recommended for production)
# DATABASE_URL="postgresql://user:password@localhost:5432/taskpilot?schema=public"

# ============================================
# CORS Configuration (Optional)
# ============================================
# Comma-separated list of allowed origins
# CORS_ORIGINS=http://localhost:3000,https://your-production-domain.com

# ============================================
# Security Notes
# ============================================
# 1. NEVER use default or predictable values in production
# 2. Keep your .env file out of version control (add to .gitignore)
# 3. Use different secrets for development and production
# 4. Rotate your JWT_SECRET periodically (invalidates all tokens)
# 5. Consider using a secrets management service in production:
#    - AWS Secrets Manager
#    - HashiCorp Vault
#    - Azure Key Vault
#    - Google Cloud Secret Manager

